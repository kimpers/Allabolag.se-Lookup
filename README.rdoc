== README

Work sample done in Ruby on Rails. Service that allows searches for company organization number with name.
Searches on http://allabolag.se, parses and caches results in a database.

Client application that connects to service and submits requests
